<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.ComposeScaffold" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- core-splashscreen 兼容主题：API 31 以下也能获得统一启动屏 -->
    <style name="Theme.ComposeScaffold.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.ComposeScaffold</item>
    </style>
</resources>
