# 保留崩溃堆栈行号，同时隐藏源码文件名。
-keepattributes SourceFile,LineNumberTable
-renamesourcefileattribute SourceFile

# kotlinx-serialization、Retrofit、OkHttp 与 MMKV 已随依赖提供 consumer rules。
# 业务新增反射或 JNI 入口时，只为明确目标补充最窄规则，禁止整包 keep。
